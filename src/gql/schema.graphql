type Query {
    author(username: String, email: String): Author
    user: User
}

type User {
    username: String!
}

type Mutation {
    newAuthor(username: String!, email: String!, password: String!): Author
}

type Author {
    id: ID!
    username: String!
    email: String!
    accountStatus: String!
}
